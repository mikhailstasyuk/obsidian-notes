Аффинная функция — это полиномиальная функция первой степени, то есть функция, график которой представляет собой прямую линию.
$$
f(x) = bx + c,
$$
где $b$ — наклон линии, а $c$ — точка пересечения линии с вертикальной осью. 